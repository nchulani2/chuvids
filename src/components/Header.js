import React from 'react';
import Checkbox from './Checkbox';
import '../styles/Header.css';

const Header = () => {
  return (
    <div style={{ textAlign: 'center' }}>
      <div className="headerPar">chuvids</div>
      <Checkbox />
    </div>
  );
};

export default Header;
